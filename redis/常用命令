  KEYS pattern 
      KEYS * 匹配数据库中所有 key 。
      KEYS h?llo 匹配 hello ， hallo 和 hxllo 等。
      KEYS h*llo 匹配 hllo 和 heeeeello 等。
      KEYS h[ae]llo 匹配 hello 和 hallo ，但不匹配 hillo 
 FLUSHDB
 删除当前数据库里面的所有数据，这个命令永远不会出现失败。
 
 =======================20140724===============
 redis持久化的方式有俩种,快照和AOF
 俩种方式各有利弊,快照性能更好但是会丢数据,AOF不会丢太多数据但是性能较慢
 
      
